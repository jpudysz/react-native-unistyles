#include "Parser.h"

using namespace margelo::nitro::unistyles;
using namespace facebook;

jsi::Object parser::Parser::parseUnistyles(jsi::Runtime &rt, std::unordered_map<std::string, Unistyle>& unistyles) {
    // todo
    
    return jsi::Object(rt);
}
